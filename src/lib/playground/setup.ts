// import { createTypeScriptSandbox, PlaygroundConfig } from './sandbox'
// import { setupPlayground } from '.'

// async function main() {
//   const monaco = await import('monaco-editor')
//   const ts = await import('typescript')
//   const sandboxConfig: Partial<PlaygroundConfig> = {
//     domID: 'andy-editor'
//   }
//   const sandbox = createTypeScriptSandbox(sandboxConfig, monaco, ts)

//   setupPlayground(sandbox, monaco, { lang: 'typescript', prefix: '' })
// }

// main()
